import { randomBytes } from 'node:crypto';

function token(bytes = 48) {
  return randomBytes(bytes).toString('base64url');
}

const secrets = {
  ELITE_AUTH_SECRET: token(),
  ELITE_METRICS_TOKEN: token(),
  ELITE_CLAIMS_WEBHOOK_SECRET: token(),
};

console.log('# Generated strong local secrets');
console.log('# Paste these into GitHub → Settings → Environments → production → Environment secrets.');
console.log('# Do not commit this output. Regenerate if the terminal history or values are exposed.');
for (const [name, value] of Object.entries(secrets)) {
  console.log(`${name}=${value}`);
}

console.log('\n# Still external / not generated by this script:');
for (const name of [
  'CLOUDFLARE_API_TOKEN',
  'CLOUDFLARE_ACCOUNT_ID',
  'DATABASE_URL',
  'R2_ACCOUNT_ID',
  'R2_BUCKET',
  'R2_ACCESS_KEY_ID',
  'R2_SECRET_ACCESS_KEY',
  'ELITE_CLAIMS_WEBHOOK_URL',
  'ELITE_LAUNCH_MANIFEST_JSON',
]) console.log(name);
