const REQUIRED=['authorize','evaluatePolicies','DurableWorkflow','assessProductionReadiness','createEvidence','verifyEvidence','routeAiRequest'];
async function fallback(){const out={};for(const p of ['./hybrid-security.mjs','./hybrid-compute.mjs']){try{Object.assign(out,await import(p));}catch{}}return out;}
export async function loadElitePlatform(){try{const sdk=await import('@elite/sdk');const missing=REQUIRED.filter(k=>!(k in sdk));if(missing.length)throw new Error(`Elite SDK missing exports: ${missing.join(', ')}`);return{source:'elite-sdk',degraded:false,platform:sdk,missing:[]};}catch(error){const platform=await fallback();return{source:'local-fallback',degraded:true,platform,missing:REQUIRED.filter(k=>!(k in platform)),reason:error instanceof Error?error.message:String(error)};}}
