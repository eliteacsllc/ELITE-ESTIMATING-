# PostgreSQL TLS Launch Gate Verification

Verification marker for the production launch-readiness hardening that requires encrypted PostgreSQL transport for non-local production databases while preserving localhost CI/dev behavior.
