name: Elite Estimating CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: elite-estimating-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DATABASE_URL: postgresql://elite@127.0.0.1:5432/elite_estimating
      ELITE_REQUIRE_RATE_LIMIT: 1
      ELITE_RATE_LIMIT_CAPACITY: 100
      ELITE_RATE_LIMIT_REFILL_PER_SECOND: 10
      PORT: 8787
    services:
      postgres:
        image: postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685
        env:
          POSTGRES_USER: elite
          POSTGRES_HOST_AUTH_METHOD: trust
          POSTGRES_DB: elite_estimating
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U elite -d elite_estimating"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - name: Generate ephemeral CI auth secret
        shell: bash
        run: |
          set -euo pipefail
          value="$(openssl rand -hex 32)"
          echo "::add-mask::$value"
          echo "ELITE_AUTH_SECRET=$value" >> "$GITHUB_ENV"
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            deploy/cloudflare/package-lock.json
      - name: Verify repository security invariants
        run: |
          node scripts/verify-action-pins.mjs
          node scripts/verify-container-pins.mjs
          bash -n scripts/configure-repository-guardrails.sh
          bash -n scripts/verify-repository-guardrails.sh
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - run: npm run verify
      - run: npm audit --omit=dev --audit-level=high
      - name: Typecheck Cloudflare Containers adapter
        working-directory: deploy/cloudflare
        run: |
          npm ci --ignore-scripts --no-audit --no-fund
          npm run check
      - name: Run database migrations
        run: npm run migrate
      - name: Smoke tenant isolation on PostgreSQL
        run: npm run smoke:postgres:tenant-isolation
      - name: Smoke distributed PostgreSQL rate limiting
        run: npm run smoke:postgres:rate-limit
      - name: Smoke atomic supplement approval transaction
        run: npm run smoke:postgres:transactions
      - name: Smoke primary database schema integrity
        run: npm run smoke:postgres:schema
      - name: Verify PostgreSQL backup and restore
        run: |
          docker run --rm --network host postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685 \
            pg_dump -h 127.0.0.1 -U elite -d elite_estimating -Fc > /tmp/elite-estimating.dump
          test -s /tmp/elite-estimating.dump
          docker run --rm --network host postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685 \
            psql -h 127.0.0.1 -U elite -d postgres -v ON_ERROR_STOP=1 \
            -c 'DROP DATABASE IF EXISTS elite_estimating_restore;' \
            -c 'CREATE DATABASE elite_estimating_restore;'
          cat /tmp/elite-estimating.dump | docker run --rm -i --network host postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685 \
            pg_restore -h 127.0.0.1 -U elite -d elite_estimating_restore --no-owner --no-privileges --exit-on-error
          DATABASE_URL=postgresql://elite@127.0.0.1:5432/elite_estimating_restore npm run smoke:postgres:schema
          PRIMARY_ESTIMATES=$(docker run --rm --network host postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685 \
            psql -h 127.0.0.1 -U elite -d elite_estimating -Atc 'SELECT COUNT(*) FROM estimates;')
          RESTORED_ESTIMATES=$(docker run --rm --network host postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685 \
            psql -h 127.0.0.1 -U elite -d elite_estimating_restore -Atc 'SELECT COUNT(*) FROM estimates;')
          PRIMARY_SUPPLEMENTS=$(docker run --rm --network host postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685 \
            psql -h 127.0.0.1 -U elite -d elite_estimating -Atc 'SELECT COUNT(*) FROM supplements;')
          RESTORED_SUPPLEMENTS=$(docker run --rm --network host postgres:16-alpine@sha256:cf78e76683b9ca8c5733cbbdce6c9262b45b6767934dd0a95e671f9a0fc20685 \
            psql -h 127.0.0.1 -U elite -d elite_estimating_restore -Atc 'SELECT COUNT(*) FROM supplements;')
          test "$PRIMARY_ESTIMATES" = "$RESTORED_ESTIMATES"
          test "$PRIMARY_SUPPLEMENTS" = "$RESTORED_SUPPLEMENTS"
          test "$RESTORED_ESTIMATES" -gt 0
          test "$RESTORED_SUPPLEMENTS" -gt 0
      - name: Smoke production server, idempotency, and remote edge flow
        run: |
          node dist/api/server.js > /tmp/elite-estimating.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID || true; cat /tmp/elite-estimating.log' EXIT
          for i in {1..20}; do
            if curl --fail --silent http://127.0.0.1:8787/ready > /tmp/ready.json; then break; fi
            sleep 1
          done
          cat /tmp/ready.json
          grep -q '"status":"ready"' /tmp/ready.json
          grep -q '"rateLimitDurable":true' /tmp/ready.json
          grep -q '"rateLimitHealthy":true' /tmp/ready.json
          curl --fail --silent http://127.0.0.1:8787/health | grep -q '"status":"ok"'
          npm run smoke:http
          ELITE_SMOKE_BASE_URL=http://127.0.0.1:8787 npm run smoke:remote
      - name: Build production container
        run: docker build -t elite-estimating:ci .
